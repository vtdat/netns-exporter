api_server:
  server_address: 0.0.0.0
  server_port: 9101
  request_timeout: 30
  telemetry_path: /metrics

internal_cidrs:
  - 10.0.0.0/8

scrape_interval: 60
log_directory: /var/log/netns-exporter

enabled_metrics:
  ping: true
